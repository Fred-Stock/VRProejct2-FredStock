using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Script is currently unused, holds data about a tower piece to aid in random generation
/// </summary>
public class TowerPieceData : MonoBehaviour
{
    
    public float height;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
